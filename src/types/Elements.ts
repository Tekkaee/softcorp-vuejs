export enum TagType {
  link = "a",
  button = "button",
  div = "div",
}

export enum FooterLinkType {
  a = "a",
  button = "button",
  div = "div",
}

export enum VariantType {
  default = "default",
  outline = "outline",
}
